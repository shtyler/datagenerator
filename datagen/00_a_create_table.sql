---------------------------------------------------------------------
---> Randomizing tables
---------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS csv_custom_values(
dataset VARCHAR, field VARCHAR, values VARCHAR
);
